# SBS 홍보용 스틸컷 원본 (56MB)
#
# 커밋하지 않는 이유:
#   1) SBS가 "무단 전재, 재배포 및 AI학습 이용 금지"를 명시함 → 공개 저장소(GitHub)에
#      올리는 것 자체가 재배포에 해당한다. 개인 참고용으로 로컬에만 둔다.
#   2) 용량(56MB)
#
# 필요하면 언제든 재생성:  node collect-stills.mjs
#
# 메타데이터(stills-manifest.json)와 수집 스크립트는 이미지가 아니므로 커밋한다.
stills/
